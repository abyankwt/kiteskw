-- No structural changes needed — cms_blocks.block_type already exists.
-- This migration is a no-op placeholder; block_type values 'color', 'image', 'select'
-- are handled purely by the frontend renderer based on existing block_type column.
SELECT 1;
